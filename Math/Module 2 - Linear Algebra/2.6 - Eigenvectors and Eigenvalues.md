# 2.6 - Eigenvectors and Eigenvalues

> [!def] Eiegenvector
> Let there be an $n\times n$ [[2.2 - Matrices#^5bf009]] $\mathbf{A}$. A [[2.1 - Vector Spaces#^f36929]] $\mathbf{x}\in \mathbb{R}^{n}$ is an **eigenvector** of $\mathbf{A}$ if it remains *a scalar multiple of itself* under the transformation induced by $\mathbf{A}$. The factor by which it is scaled is the [[#^888169]] $\lambda\in \mathbb{R}$.
> $$\mathbf{A}\,\mathbf{x}\equiv\lambda\,\mathbf{x}$$
>

^b325d4

> [!def] Eiegenvalue
> Let there be an $n\times n$ [[2.2 - Matrices#^5bf009]] $\mathbf{A}$ with an [[#^b325d4]] $\mathbf{x}$. The **eigenvalue** $\lambda\in \mathbb{R}$ is the *factor* by which $\mathbf{x}$ is *scaled* under the transformation induced by $\mathbf{A}$.
>
> $$\mathbf{A}\,\mathbf{x}\equiv\lambda\,\mathbf{x}$$
>

^888169

> [!def] Spectrum
> Let there be a matrix $\mathbf{A}$. The set of every [[#^888169]] of $\mathbf{A}$ is the **spectrum** $\sigma(\mathbf{A})$.
>
> $$\sigma(\mathbf{A})=\{\lambda_{1},\dots,\lambda_{m} \}$$
>

^b87cb6

## Eigenspace

> [!def] Basic Eigenvector
> The [[2.2 - Subspaces#^992f04]] comprised of each [[#^b325d4]] is a **basic eigenvector**.

^90b851

> [!def] Eigenspace
> For a given [[#^888169]] $\lambda$ of $\mathbf{A}$, the eigenspace $E_{\lambda}(\mathbf{A})$ is the set of each [[#^b325d4]] of $\mathbf{A}$. Furthermore, the eigenspace is a [[2.2 - Subspaces#^420b21]].
> $$E_{\lambda}(\mathbf{A})=\{\mathbf{x}:\mathbf{A}\mathbf{x}=\lambda\,\mathbf{x} \}$$

^8c43f5

> [!thm] Eigenvectors of the Inverse
> Let there be a matrix $\mathbf{A}$ with each [[#^888169]] $\lambda_{i}$ and [[#^b325d4]] $\mathbf{x}_{i}$. Let $\mathbf{A}$ have an existing [[2.2 - Matrices#^a2f166]] $\mathbf{A}^{-1}$. The eigenvectors and eigenvalues of $\mathbf{A}^{-1}$ are $\lambda^{-1}, \ \mathbf{x}^{-1}$.

### Multiplicity

> [!def] Algebraic Multiplicity
> The algebraic multiplicity of an [[#^888169]] $\lambda$ is number of times it **repeats**. For $k$ eigenvalues, let the [[#^223c29]] be factored as $p_{c}=(x-\lambda_{1})^{n_{1}}\dots(x-\lambda_{k})^{n_{k}}=0$. The exponent $n_{i}$ is the algebraic multiplicity of $\lambda_{i}$.
> $$AM_{\lambda_i}=n_{i}$$

^dbcea1

> [!def] Degenerate Eigenvalue
> A **degenerate** [[#^888169]] has a non-zero [[#^dbcea1]] and is *not unique*.

^98842b

> [!def] Geometric Multiplicity
> The geometric multiplicity of an [[#^888169]] $\lambda$ is the [[2.2 - Subspaces#^09a800]] of the [[#^8c43f5]].

^323406

> [!thm]
> For a given [[#^888169]] $\lambda$ of $\mathbf{A}$, the [[#^dbcea1]] is always greater then or equal to [[#^323406]], which is always greater than $1$.
> $$AM_{\lambda}\geq GM_{\lambda}>1$$

## Characteristic Polynomial

> [!def] Characteristic Polynomial
> Let there be an [[#^888169]] $\lambda$ and [[#^b325d4]] $\mathbf{x}$ of a matrix $\mathbf{A}$. By definition, $\mathbf{A}\,\mathbf{x}=\lambda\,\mathbf{I}$. The characteristic polynomial $p_{c}(x)$ is the [[2.4 - Determinants#^add7e3]] of $\lambda\,\mathbf{I}-\mathbf{A}$.
> $$p_{c}(x)=\det(x\,\mathbf{I}-\mathbf{A})$$

^223c29

> [!thm] Eigenvalues from Characteristic Polynomial
> Each root $x=\lambda$ of the [[#^223c29]] is an [[#^888169]].
> $$\det(\lambda\,\mathbf{I}-\mathbf{A})=0$$

^6f4a26

`\begin{proof}`@[[#^6f4a26]]
`\end{proof}`
If $\mathbf{A}\,\mathbf{x}=\lambda\,x$, then for some non-zero eigenvector $\mathbf{x}$ and eigenvalue $\lambda$,

$$
(\lambda\,\mathbf{I}-\mathbf{A})\,\mathbf{x}=\mathbf{0}
$$

^010736

Suppose $(\lambda\,\mathbf{I}-\mathbf{A})^{-1}$ exists. Then,

$$
\begin{align}
\mathbf{x}&=\Big((\lambda\,\mathbf{I}-\mathbf{A})^{-1}\,(\lambda\,\mathbf{I}-\mathbf{A})\Big)\,\mathbf{x} =(\lambda\,\mathbf{I}-\mathbf{A})^{-1}\,\Big((\lambda\,\mathbf{I}-\mathbf{A})\,\mathbf{x}\Big) \\ \\
\end{align}
$$
However, substituting [[#^010736]], we see that $\mathbf{x}=(\lambda\,\mathbf{I}-\mathbf{A})^{-1}\,(\mathbf{0})=\mathbf{0}$. We have proved that $(\lambda\,\mathbf{I}-\mathbf{A})^{-1}$ *does not exist* by contradiction. Therefore, by first statement of the [[2.4 - Determinants#^464a38]] theorem, we know that $\det(\lambda\,\mathbf{I}-\mathbf{A})=0$.

> [!thm] Eigenvectors from Null Space
> Each [[#^90b851]] $\mathbf{x}$ is a [[2.2 - Matrices#^268161]] of $\lambda\,\mathbf{I}-\mathbf{A}$.
>
> $$E_{\lambda}(\mathbf{A})=ker(\lambda\,\mathbf{I}-\mathbf{A})$$

## Eigenbasis

> [!def] Eigenbasis
> The **eigenbasis** is a [[2.2 - Subspaces#^ffe859]] formed by each [[#^90b851]] $\mathbf{x}_{i}$ of $\mathbf{A}$.
>
> $$E_{\lambda}(\mathbf{A})=span(\{\mathbf{x}_1,\dots,\mathbf{x}_{n} \})$$
>
>

^1a2381

> [!def] Orthonormal Eigenbasis
> A [[2.2 - Subspaces#^c6eb2b]] that is also an [[#^1a2381]].
>
>

^1a2381

### Changing Basis

> [!thm] Vector in the Eigenbasis
> A [[2.1 - Vectors#^41fef0]] $\ket{v} \in \mathbb{C}^{n}$ can be expressed in the [[2.6 - Eigenvectors and Eigenvalues#^1a2381]] $\{\ket{a_1}, \ldots, \ket{a_n}\}$ of $\mathbf{A}$ as:
>
> $$\ket{v} = \begin{bmatrix}
  \braket{a_{1} | v} \\
\vdots \\
\braket{a_{n} | v}
\end{bmatrix}$$
>
>

^1fbbef

`\begin{proof}`@[[#^1fbbef]]
Insert the [[2.1 - Vector Spaces#^3e433d]] $\mathbf{I} = \sum_{i=1}^{n} \ket{a_i}\bra{a_i}$:

$$
\begin{align}
\ket{v} = \mathbf{I}\ket{v} &= \left(\sum_{i=1}^{n} \ket{a_i}\bra{a_i}\right)\ket{v} \\
&= \sum_{i=1}^{n} \ket{a_i}\Big(\bra{a_i}\ket{v}\Big) = \sum_{i=1}^{n} \Big(\bra{a_i}\ket{v}\Big)\,\ket{a_i}
\end{align}
$$

This decomposition expresses $\ket{v}$ as a linear combination of eigenvectors $\ket{a_i}$. Each coefficient $v_{i}$ in this representation is the [[2.1 - Vectors#^0ab8b3]] $v_i=\braket{a_{i} | v}$.
`\end{proof}`

> [!thm] Matrix in the Eigenbasis
> A [[2.2 - Matrices#^5bf009]] $\ket{v} \in \mathbb{C}^{n}$ can be expressed in the [[2.6 - Eigenvectors and Eigenvalues#^1a2381]] $\{\ket{a_1}, \ldots, \ket{a_n}\}$ of $\mathbf{A}$ as:
>$$\mathbf{B}=\begin{bmatrix}
\bra{a_{1}}\mathbf{B}\ket{a_{1}}& \dots & \bra{a_{1}}\mathbf{B}\ket{a_{n}} \\
\vdots & \vdots & \vdots \\
\bra{a_{n}}\mathbf{B}\ket{a_{1}} & \dots & \bra{a_{n}}\mathbf{B}\ket{a_{n}}
\end{bmatrix}$$

^a6e5f2

`\begin{proof}`@[[#^a6e5f2]]
Insert the [[2.1 - Vector Spaces#^3e433d]] $\mathbf{I} = \sum_{i=1}^{n} \mathbf{x}_i\mathbf{x}_i^{\dagger}$ on both sides of $\mathbf{B}$:

$$
\begin{align}
\mathbf{B} = \mathbf{I}\,\mathbf{B}\,\mathbf{I} &= \left(\sum_{i=1}^{n} \ket{a_i}\bra{a_i}\right)\mathbf{B}\left(\sum_{j=1}^{n} \ket{a_j}\bra{a_j}\right) \\
&= \sum_{i=1}^{n}\sum_{j=1}^{n} \ket{a_i}\Big(\bra{a_i}\mathbf{B}\ket{a_j}\Big)\bra{a_j} = \sum_{i=1}^{n}\sum_{j=1}^{n} \Big(\bra{a_i}\mathbf{B}\ket{a_j}\Big)\,\ket{a_i}\bra{a_j}
\end{align}
$$

The term in parentheses represents the matrix $\mathbf{B}$ in the eigenbasis. Each element $B_{ij}$ in this representation is computed by projecting $\mathbf{B}$ between basis vectors: $B_{ij} = \bra{a_i}\mathbf{B}\ket{a_j}$. The set $\{\ket{a_i}\bra{a_j}\}$ for all $i,j$ spans the space of $n \times n$ matrices, just as the $span(\{\ket{a_i}\})(=\mathbb{C}^n$. So,

$$
\mathbf{B}=\begin{bmatrix}
\bra{a_{1}}\mathbf{B}\ket{a_{1}}& \dots & \bra{a_{1}}\mathbf{B}\ket{a_{n}} \\
\vdots & \vdots & \vdots \\
\bra{a_{n}}\mathbf{B}\ket{a_{1}} & \dots & \bra{a_{n}}\mathbf{B}\ket{a_{n}}
\end{bmatrix}
$$
`\end{proof}`
