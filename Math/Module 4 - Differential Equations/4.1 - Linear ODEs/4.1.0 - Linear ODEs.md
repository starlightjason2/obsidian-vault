# 4.1.0 - Linear ODEs

> [!def] Linear ODE
> A **linear** [[4.0 - ODEs#^ode]] of order $n$ can be expressed as a [[2.7 - Function Spaces#^linear-operator]] $\mathcal{L}$, and is of the form
> $\mathcal{L}(x)\equiv\sum_{i=0}^{n}{p_{i}(x)\,y^{(i)}(x)}=f(x)$

^linear-ode

> [!def] Linear Homogeneous ODE
> A **homogeneous** [[#^linear-ode]] has $f(x)=0$ on the right. The homogeneous counterpart of any non-homogenous ODE is often called it's *complementary* equation.
>
>
> $$\sum_{i=0}^{n}{p_{i}(x)\,y^{(i)}(x)}=0$$

^linear-homogeneous-ode

## Homogeneous Solution Space

> [!def] Fundamental Solutions
> The solutions to an order $n$ [[#^linear-homogeneous-ode]] form a [[2.2 - Matrices#^null-space]]. The [[2.2 - Subspaces#^basis]] solutions $\{y_{1}, \, \dots, \, y_{n}\}$ are called the **fundamental solutions** and the homogeneous solution $y_{h}(x)$ is any function spanned by the fundamental solutions.
>
> $$y_{h}(x)=\sum_{i=0}^{n}{c_{i}\,y_{i}(x)}$$

^fundamental-solutions

### Wronskian

> [!def] Wronksian
> Let there be [[#^fundamental-solutions]] $\{y_{1}(x), \, \dots, \ y_{2}(x) \}$. The **Wronskian** $W(x)$ is the [[2.4 - Determinants#^determinant]] of the matrix formed by each order [[3.5 - Derivatives#^derivative]].
>$$
>W(x)\equiv\begin{vmatrix}
y_{1}(x) & \dots & y_{n}(x) \\
\vdots & \vdots & \vdots \\
(y_{n})^{{n}}(x) & \dots & (y_{n})^{{n}}(x)
\end{vmatrix}
>$$

^adbb90

> [!thm] Wronksian Test
> A solution set is a [[#^fundamental-solutions]] if the [[#^adbb90]] is a [[2.4 - Determinants#^non-zero-determinant]].
> $$\{y_{1}(x), \, \dots, \ y_{n}(x) \} \text{ is a fundamental set}\iff W(x)\neq0$$

^wronksian-test

`\begin{proof}`@[[#^wronksian-test]]
Notice that $y''+p(x)\,y'+q(x)\,y=0\implies y''=-p(x)\,y'-q(x)\,y$, letting us turn a *single second order* ODE into a system of *two first order* ODEs.

$$
\begin{align}
-q(x)\,y-p(x)\,y'&=y'' \\
y'&=y'
\end{align}\iff\begin{bmatrix}
-q(x) & -p(x) \\
0 & 1
\end{bmatrix}\begin{bmatrix}
y \\
y'
\end{bmatrix}=\begin{bmatrix}
y'' \\
y'
\end{bmatrix}
$$

Check if $y_{1}(x)$ and $y_{2}(x)$ are independent using the [[2.2 - Subspaces#^78b930]]. If so, they form a basis for $y_{h}=span(\{y_{1},y_{2} \})$.

$$
\begin{vmatrix}
y_{1} & y_{2} \\
y_{1}' & y_{2}'
\end{vmatrix}=W(x)>0\iff y_{h}=span(\{y_{1},y_{2} \})
$$

`\end{proof}`

## General Solution

> [!thm] Unique Solution for Linear IVP
> Given any [[#^linear-ode]], if each function $p_{i}(x)$ is a [[3.4 - Limits#^continuous-function]] over $x_{0}\in(a,b)$, then an [[4.0 - ODEs#^initial-value-problem]] for $n$ initial conditions $\{y^{(n-1)}=k_{n-1} \}$ has a unique solution.
>

> [!thm] General Solution of Linear ODE
> The general solution $y(x)$ to a [[#^linear-ode]] is any **particular solution** $y_{p}(x)$ plus the [[4.1.0 - Linear ODEs#^fundamental-solutions]] of the corresponding homogeneous ODE.
>
> $$y(x)= y_{p}(x) + y_{h}(x)$$

^02185f
